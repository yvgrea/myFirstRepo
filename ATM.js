//ATM dispensor

const wantCash = (account,cashRequest)=>{
    if(cashRequest >=20 && account>=20)
        console.log("Dispensing the money honey")
    }

const pinNumber =(num)=>{
    if (pin == true)
    
        console.log("proceed to take out cash")}
        {
    else 
        console.log("try again")

        }
 